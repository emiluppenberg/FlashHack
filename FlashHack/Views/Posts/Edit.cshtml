@model FlashHack.Models.Post

@{
    ViewData["Title"] = "Edit";
    @inject IHttpContextAccessor HttpContextAccessor

}

<div class="welcome-section">
    <div class="row">
        <div class="col-md-8">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Id" />
                <input type="hidden" asp-for="UpVotes" />
                <input type="hidden" asp-for="DownVotes" />
                <input type="hidden" asp-for="TimeCreated" />
                <input type="hidden" asp-for="Comments" />
                <input type="hidden" asp-for="UserId" value="@Model.UserId" />
                <input type="hidden" asp-for="SubCategoryId" />
                <div class="form-group">
                    <label asp-for="Title" class="control-label"></label>
                    <input asp-for="Title" class="form-control" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Content" class="control-label"></label>
                    <textarea asp-for="Content" class="form-control" rows="10"></textarea> <!-- Changed to textarea and added rows attribute -->
                    <span asp-validation-for="Content" class="text-danger"></span>
                </div>
                @* @if(HttpContextAccessor.HttpContext.Session.GetString("IsAdmin") == "True")
            {
                <div class="form-group">
                    <label asp-for="SubCategoryId" class="control-label">SubCategory</label>
                    <select asp-for="SubCategoryId" class="form-control" asp-items="ViewBag.SubCategory"></select>
                    <span asp-validation-for="SubCategoryId" class="text-danger"></span>
                </div>
            } *@
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-sm btn-custom-dark solid-line mt-2" />
                </div>
            </form>
        </div>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
